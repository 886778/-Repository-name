[project]
name = "ai-bazi-backend"
version = "0.0.0"
requires-python = "==3.14.6"
dependencies = [
  "asyncpg>=0.31,<1",
  "redis>=8,<9",
]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"
